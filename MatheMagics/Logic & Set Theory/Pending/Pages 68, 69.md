Mathematical theories' alphabet.
Make corrections.
# Alphabets & Denotations (for theory pages)

| Field of Study       | Denotation | Alphabet (theory symbols)                                  | Notes |
|----------------------|------------|-------------------------------------------------------------|-------|
| Set Theory           | ST         | Predicate `∈`                                               | Membership is the core non-logical symbol. |
| Group Theory         | GT         | Constant `e`; unary `()^{-1}`; binary `\cdot`              | Identity, inverse, and group operation. |
| Ring Theory          | RT         | Constants `0, 1`; unary `-`; binary `+`, `\cdot`           | Standard ring language (commutative with `1` by default; adjust if needed). |
| Number Theory        | NT         | Constants `0, 1`; binary `+`, `\cdot`; (often) predicate `\le` | “Arithmetic” language over ℕ/ℤ; some presentations omit `\le`. |
| Peano Arithmetic     | PA         | Constant `0`; unary `S`; binary `+`, `\cdot`; (optional) `\le` | Classic PA language with successor `S`. |
| Arithmetic           | A          | Constants `0, 1`; binary `+`, `\cdot`                      | If you keep “A” distinct from PA/NT, use this minimal arithmetic language. |
| AR                   | AR         | *(per O’Leary, pp. 68–69 — fill exactly as listed)*        | Placeholder to match the book’s specific symbols. |
| AR′                  | AR'        | *(per O’Leary, pp. 68–69 — fill exactly as listed)*        | Placeholder to match the book’s specific symbols. |
