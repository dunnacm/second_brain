Kaizen: In numbered expressions/lines, after the step label (e.g., (3)), all eligible tokens—including variables like x in G(x), M(x)—should be wrapped in \mathtt where appropriate; broadly, everything that can be in \mathtt should be in \mathtt after numbering, without spreading onto convention-bound symbols.

In MatheMagics, “rule of indentation” means: within each equation/derivation set, align the biconditional/implication markers by placing `&` immediately before `\Leftrightarrow` (or similar marker) on every line; continuation lines use an empty left column so the marker stacks vertically without manual `\qquad` spacing.

For Wondershare EdrawMind mindmap imports, use plain Markdown hierarchy with headings (#, ##, ###, ####) and bullet lists (“- …”) exactly like the user’s example; do not use OPML/XML.

Will type "end" when they want to start a new chat; assistant should then provide a copy-paste project overview to carry over MATHEMAGICS context and formatting protocols for seamless continuity.

Kaizen update: For “conditionals parsing” statements and similar notes, use symbol-level monospace formatting: wrap only propositional-variable tokens in `\mathtt{...}` (e.g., `\mathtt{a}\to\mathtt{b}\to\mathtt{c}`), while leaving connectives like `\to` and parentheses in normal math fonts; keep the explanatory phrase in `\text{...}` and use display math `$$...$$` when requested.

Kaizen update (Obsidian theorem-form notes): In theorem/definition callouts using the IF/THEN template, put setup lines like “$p,q,r$ are propositional forms” inside the **IFF** section as bullet hypotheses (not as separate prose outside the IFF block). For these semantic-consequence notes, format the IFF condition as inline math (e.g., `$\models ext{,}(p ext{,}q) o r.$`) and reserve display math `$$...$$` for the THEN/conclusion line (e.g., `$$p,q ext{,} ext{models} r.$$`). Preserve the user’s exact spacing/line-break conventions for Obsidian rendering (including trailing two spaces where used, as in the “Compact bigwedge form” variant).